# KAito LAb

[Lab Link](https://azure-samples.github.io/aks-labs/docs/ai-workloads-on-aks/kaito-ragengi)

## KAITO workspace

- KAITO workspaces are Kubernetes custom resources that are used to manage open-source model inference deployments within your cluster

Purpose: Automating provision of GPU nodes

- Automating and orchastrating
  - Automate and operationalise AI workloads for you
- Need a federated identity credential for KAITO
- Requires a GPU provisioner and KAITO workspace

## KAITO RAGEngine

- KAITO RAGEngine is another Kubernetes custom resource that simplifies the deployment and management of Retrieval Augmented Generation (RAG) workloads
- Allows you to easily deploy RAG applications and workflows that can retrieve information from external knowledge sources and use it to enhance the responses generated by large language models (LLMs)
- In a typical RAG workflow, you have a knowledge base (like a database or an index) and an LLM. Data is often indexed in a vector database, which allows for efficient retrieval of relevant information based on the user's query. This requires a few key components:
  - **Knowledge Base**: A source of information that can be indexed and queried to retrieve relevant data.
  - **Vector Database**: A database that stores the indexed data in a format that allows for efficient similarity search.
  - **Embedding model**: A model that converts text into vector representations, which are then stored in the vector database.
  - **Inference Service**: A service that provides access to a large language model (LLM) for generating responses based on the retrieved information.
- KAITO RAGEngine, the deployment of these components is simplified by providing a single CRD that encapsulates the necessary configurations for the vector database, embedding model, and inference service.
  - Default settings are for it to use the Faiss vector database and the BAAI general embedding (bge) model, but it can be configured to use other embedding models as needed

## Use Case

- You want to build a chatbot that can answer questions about the products available in the store.
- We can start to build a RAG enabled chatbot that is more likely to recommend products based on the product data available in the store and not recommend things that aren't relevant.
- To do this, you will need to index the product data in a format that the RAGEngine can understand.

### Data preperation

- To use the RAGEngine, data must be indexed in a specific format that the RAGEngine can understand
- Example:
- The `index_name` is the name of the index where the documents will be stored.
- The `documents` array contains individual documents, each with a `text` field for in this case the product description, and a `metadata` field that can include additional information such as the author, category, and URL of the product

```json
{
  "index_name": "store_index",
  "documents": [
    {
      "text": "Product description",
      "metadata": {
        "author": "Contoso Pet Supply",
        "category": "Product category",
        "url": "http://store-ip/product/product-id"
      }
    }
  ]
}
```

### Fetching Product Data

- The sample e-commerce application provides a REST API to fetch product data.
- Need to get get the external IP address of the store front service
- `$env:STORE_IP=$(kubectl get service store-front -ojsonpath='{.status.loadBalancer.ingress[0].ip}')`
- `curl http://${STORE_IP}/api/products | jq` or `Invoke-RestMethod -Uri "http://$env:STORE_IP/api/products" | ConvertTo-Json -Depth 10`
- should see a JSON response containing the product data, which includes fields like id, name, description, and price.

### Transforming Product Data for Indexing

- Need to transform this product data into a format suitable for indexing with the RAGEngine.
- extract the relevant fields and format them according to the schema we defined earlier
- `cat store_products.json `

```
curl.exe "http://$env:STORE_IP/api/products" |
  jq --arg store_ip "$env:STORE_IP" '{
    index_name: "store_index",
    documents: [
      .[] | {
        text: "\(.name) - \(.description) Price: $\(.price)",
        metadata: {
          author: "Contoso Pet Supply",
          category: (
            if (.name | test("cat|feline|kitty"; "i")) then "Cat Toys"
            elif (.name | test("dog|doggy"; "i")) then "Dog Toys"
            elif (.name | test("bed"; "i")) then "Pet Beds"
            elif (.name | test("life jacket|jacket"; "i")) then "Pet Accessories"
            else "Pet Toys"
            end
          ),
          url: "http://\($store_ip)/product/\(.id)"
        }
      }
    ]
  }' > store_products.json
  ```

  ### Indexing with RAGEngine

- now product data is in the correct format, we will index it using the RAGEngine instance deployed earlier
- done by sending a POST request to the /index endpoint of the RAGEngine instance.
- `kubectl port-forward svc/ragdemo 8080:80 &` ->  port-forward the RAGEngine service to your local machine
- 

```
curl -X POST http://localhost:8080/index \
-H "Content-Type: application/json" \
-d @store_products.json | jq
```